<resources>
    <!-- Base theme: AppCompat NoActionBar so the Compose Activity takes full control -->
    <style name="Theme.FitTrack" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
